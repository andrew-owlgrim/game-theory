import { css } from "styled-components";

// Sizes

const baseSize = 20;

const bases = (factor) => {
  return baseSize * factor;
};

const space 

// Grid

const grid = {
  desktop: {}
}

// Typography

const typography = (style, size, weight) => {
  return css`
    ${textStyles[style]}
    ${size &
    css`
      font-size: ${bases(size)};
    `}
  `;
};

// Colors



const hsbToHex = (h, s, b) => {
  return
}

const alphaHex = (hex, alpha) => {
  return 
}

// Theme

const theme = {
  screen: "",
  colors: {},
  typography: {}
}
